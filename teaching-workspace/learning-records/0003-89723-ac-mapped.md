# Learning Record 0003: 89723 Acceptance Criteria Mapped

**Date:** Sep 1, 2026  
**Session:** 3  
**Topic:** Ticket 89723 → files and verification

## What Was Learned

### Template packages
- **model-registry** — upstream UI import + federated package structure
- **agent-ops** — AI Hub / Agents tab, routes, feature flags via `extensions.ts`
- **Spike 81066 branch** — OpenShell-specific providers and `setApiBasePath`

### Host integration pattern
- Packages export `./extensions` → host auto-discovers (no manual route registry)
- Feature gating: `app.area` + `flags.required` + `frontend/src/concepts/areas/const.ts`
- API proxy: `package.json` → `module-federation.proxy` → backend forwards to BFF

### 89723 scope boundaries
- Mock BFF OK; no live gateway, no cluster manifests, no auth UX/Cypress
- Ports: local 9113, sidecar 8943 (distinct from agent-ops 8843)
- Browser path: `/openshell/api/...` on host origin

### Implementation order
1. Scaffold package → 2. MF config → 3. Register area/flag → 4. Wrapper → 5. List page → 6. Detail route → 7. Proxy verify → 8. Build

## Next Steps
- Lesson 5: Host wrapper + mock BFF hands-on
- Checkout spike branch and diff against checklist
